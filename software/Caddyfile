{
	admin 127.0.0.1:2019
}

# Start-Konfiguration: Panel über die Server-IP erreichbar.
# Sobald im Setup eine Verwaltungs-Domain gesetzt wird, schreibt weblab diese Datei neu
# (inklusive HTTPS für die Domain und je installierter Web-App eine eigene Domain).
http:// {
	reverse_proxy 127.0.0.1:8099
}
